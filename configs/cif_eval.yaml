load_models_dir: 'saves/Saved_models/'
embs_dir: 'saves/embeddings/'
plots_dir: 'saves/plots/'
data_dir: 'data/ShapeNet/shapenet.npz'

n_flows_G: 10
n_flows_F: 10
n_neurons: 512
emb_dim: 64

n_samples: 10
n_points: 2048
id4recon: 3

start_ids: !!python/tuple [0,1,3,5]
stop_ids: !!python/tuple [1,3,5,0]
n_midsamples: 10

batch_size: 2

use_EMD: False

####### PointFlow-related args #######

# these two does not influence the result, they're neede for comptability
# with point flow loaders
tr_sample_size: 1024
te_sample_size: 1024
#

root_dir: 'data/ShapeNetCore.v2.PC15k'
normalize_per_shape: false
normalize_std_per_axis: false
scale: 1.0
categories: ["chair"]
resume_dataset_mean: 'saves/Saved_models/train_set_mean.npy'
resume_dataset_std: 'saves/Saved_models/train_set_std.npy'
